#!/usr/bin/env node

/**
 * Script de inicialização do servidor MCP-Booster
 */

const { initializeServer } = require('../dist/index.js');

// Inicializar o servidor com configurações padrão
initializeServer(); 